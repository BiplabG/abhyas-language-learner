{
  "name": "abhyas-language-learner",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "lint": "web-ext lint --source-dir extension",
    "package": "npm run build && web-ext build --source-dir extension --artifacts-dir artifacts --overwrite-dest",
    "test:ui": "node tests/ui.mjs",
    "check": "npm run build && npm test && npm run lint && npm run test:ui",
    "release": "npm run check && npm run package && node scripts/release.mjs"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "fzstd": "^0.1.1",
    "sql.js": "^1.14.2",
    "ts-fsrs": "^5.4.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "esbuild": "^0.28.2",
    "prettier": "^3.9.6",
    "web-ext": "^10.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
